exports.generateText = (name, email, subject, message) => {
    // Returns output text
    return `${name} ${email} ${subject} ${message}`;
  };
  
  exports.createElement = (type, text, className, attrs) => {};